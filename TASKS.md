# Tasks

Tracks work items for Greyvetro Studio. IDs are sequential (`TASK-001`, `TASK-002`, ...) and never reused.

| ID | Description | Acceptance Criteria | Difficulty |
|----|--------------|----------------------|------------|
| TASK-001 | `frontend-web` has no client-side routing: switching sidebar tabs (`App.tsx:39` `useState<Tab>`) and opening any modal never changes the browser URL, so there's no deep-linking or back/forward support. Add a router (e.g. `react-router-dom`) mapping each sidebar tab to a distinct URL path, while preserving `Composer`'s "stays mounted across tab switches" behavior. | Clicking each sidebar item updates the URL to a distinct path per tab.<br>Browser back/forward navigates between tabs correctly.<br>Reloading the page on a tab's URL (e.g. `/gallery`) opens directly into that tab instead of resetting to Studio.<br>Composer's state-preservation across tab switches still works.<br>Existing cross-screen flows (Gallery "Edit & regenerate" → Studio, preset "Use" → Studio) still work. | Hard |
| TASK-002 | Gallery tab errors/hangs on navigation. Root cause is one or both of: (a) `GalleryScreen.tsx:64-65`'s unguarded `.then()` chains reject silently when `migrateFromOldDb()` (`db.ts:46-66`, no try/catch around its IndexedDB probe) fails, leaving the screen stuck on its spinner; (b) `settingsSummary()` (`types.ts:84-86`) / `clipTitle()` (`GalleryScreen.tsx:86`) throw on legacy records missing expected fields, with no `ErrorBoundary` to contain it. | Live repro confirms the exact failure (console error) before fixing.<br>`migrateFromOldDb()` failures are caught/logged and don't block `openDb()`.<br>`listProjects()`/`listGallery()` rejections surface an error state (toast) instead of an infinite spinner.<br>`settingsSummary()`/`clipTitle()` tolerate missing/legacy fields without throwing.<br>Gallery tab loads cleanly with the user's current data, no console errors. | Medium |
| TASK-003 | In the Timeline editor, splitting a clip (`S` key / Split button) works for photo clips but not video clips. Static review of `splitClip` (`timelineOps.ts:171-210`), `TimelineEditor.tsx`'s split gating, and `FilterGraphCompiler.cs` shows the logic already correctly supports video (inPoint/outPoint branch, no excluding guard, backend already emits per-clip inputs) — the bug doesn't reproduce from code alone and needs a live repro to find the actual divergence. | Live repro identifies the actual divergence point (selection, click target, or edge-case math).<br>Selecting a video clip on the base track allows Split exactly like a photo clip.<br>Splitting a video clip yields two clips with correct `inPoint`/`outPoint`, `duration`, and `startTime`.<br>Exported render of a split video clip plays back seamlessly across the cut.<br>Existing photo-split tests/behavior unaffected; new test coverage added for a video split (mirroring the existing photo two-clips-one-source backend test). | Medium |
| TASK-004 | Flutter's Timeline editor (`frontend/lib/features/timeline/`) has no clip transitions, even though the model and backend already support them end-to-end: `model/timeline.dart`'s `Transition`/`TransitionStyle` classes and `TimelineClip.transitionIn`/`fadeInFromBlack`/`fadeOutToBlack` fields already serialize correctly, and `FilterGraphCompiler.cs:13-22` already builds `xfade` crossfades and edge fade-to-black — confirmed shipped end-to-end on the web frontend. Only `timeline_ops.dart` (no `setClipTransition`/fade-to-black ops exist) and `timeline_screen.dart`'s UI (no boundary control) are missing. | New pure ops in `timeline_ops.dart`: `setClipTransition` (style + duration, clamped against both adjacent clips' own duration), `setFadeInFromBlack`/`setFadeOutToBlack` restricted to the first/last base-track clip only (mirroring `FilterGraphCompiler.cs:113-114`'s own `i==0`/`i==Count-1` restriction).<br>A boundary control between two adjacent base-track clips lets the user pick a style (dissolve / fade-to-black) and duration; clip bars visually overlap once set.<br>Fade-in/out-from-black sliders appear only on the first/last base-track clip's Reframe inspector.<br>`splitClip` drops transition/fade-to-black fields from whichever half of a split clip is no longer the boundary/first/last clip.<br>A real `/render` export of a two-clip crossfade and a fade-in-from-black frame-samples the expected blend/black-to-color ramp.<br>Unit tests cover the new ops; `flutter analyze` clean, `flutter test` green. | Medium |
| TASK-005 | Overlay clips (`addOverlayImage`, `timeline_ops.dart:283`) get a fixed `duration` equal to `timeline.totalDuration` at add-time — a one-time snapshot — with no way to change it afterward; `trimClip` (`timeline_ops.dart:141`) is hard-scoped to `baseVisualTrack` and rejects any other track's clip id, so the only way to shorten an overlay today is remove-and-re-add via `removeTrack` (`timeline_ops.dart:322`). Add end-trim editing for overlay clips, mirroring the web frontend's equivalent behavior (per CLAUDE.md: "Overlay clips edit like music: one clip, end-trim only"). | A new pure op (or generalization of `trimClip`) shortens/lengthens an overlay clip's `duration` from its end only, clamped to `minClipDuration` and the current timeline length.<br>The overlay lane in the Timeline tab shows a trim handle reusing the existing `_TrimDrag` drag/commit interaction.<br>Trimming an overlay clip doesn't affect the base visual track, audio track, or any other overlay track.<br>A real `/render` export confirms the overlay only composites for its (now-shorter) window.<br>Unit tests added; `flutter analyze` clean, `flutter test` green. | Medium |
| TASK-006 | `_rebuildFromStoryboard` (`timeline_screen.dart:343`) fully replaces the timeline with a fresh `seedTimelineFromScenes` result, discarding every overlay/PiP track — its own confirm dialog says so explicitly ("including any reframing, trims, and added overlays"). Web's `mergeAddedMedia` (`frontend-web/src/features/timeline/model/timelineOps.ts:597`) re-attaches non-base tracks (video/music/overlay) from the previously-saved timeline onto a freshly reseeded one, while intentionally still letting per-clip reframe/crop on regenerated scene clips reset (matching the dialog's own copy). Port an equivalent for Flutter, scoped to what exists today (overlay tracks); extend it once video/music ingestion (TASK-007/008) land. | A new pure `mergeAddedMedia`-equivalent op in `timeline_ops.dart` re-attaches every overlay track (any visual track above the saved timeline's base zIndex) from the previous timeline onto a freshly seeded one, unchanged.<br>`_rebuildFromStoryboard` calls the merge instead of using the seeded timeline directly.<br>The confirm dialog copy no longer claims overlays are lost.<br>Rebuilding a timeline with two overlays and a changed scene count keeps both overlays intact and correctly positioned/sized.<br>Unit tests cover the merge op; `flutter analyze` clean, `flutter test` green. | Easy |
| TASK-007 | Flutter's timeline has a single voiceover audio clip and no way to add background music/SFX, even though the model already supports it — `Track.volume`/`muted` and `Clip.volume`/`fadeIn`/`fadeOut` already exist on `model/timeline.dart`'s `Track`/`TimelineClip`, and the backend's multi-track audio mix (`amix`/`afade`/`adelay`/`apad`, `FilterGraphCompiler.cs:123-201`) already handles any number of audio clips beyond the voiceover — confirmed shipped end-to-end on web (`addMusic`/`setTrackAudio`/`setClipFade` in `frontend-web/src/features/timeline/model/timelineOps.ts`). | New pure ops in `timeline_ops.dart`: `addMusicTrack` (new audio track/clip clamped to the current timeline length, mirroring `addOverlayImage`'s shape), `setTrackAudio` (volume/mute), `setClipFade` (fade in/out); `trimClip` extended to also cover music clips.<br>"🎵 Add music" action on the Timeline tab file-picks an audio file, stores its blob via `TimelineAssetRepository`, and adds a selectable clip with a volume/mute/fade inspector (mirroring the Overlay inspector's shape).<br>A real `/render` export with voiceover + one music clip produces the expected mixed audio, level-sampled.<br>TASK-006's merge op is extended to also carry music tracks across a "Rebuild from storyboard" resync.<br>Unit tests cover the new ops; `flutter analyze` clean, `flutter test` green. | Medium |
| TASK-008 | Flutter's timeline is stills-only — `timeline_ops.dart:3` and `timeline_screen.dart:151`'s doc comments both note "no video/music tracks... yet" — while the backend compiler already fully supports trimmed real-video source clips merged into the base concat (`FilterGraphCompiler.cs:79-238`, `InPoint`/`Duration`-based `-ss`/`-t` trim) and the Dart model already has everything a video clip needs (`MediaAsset(type: MediaType.video, duration, width, height)`, `TimelineClip.inPoint`/`outPoint`) — confirmed shipped end-to-end for web (`appendVideoClip`, `frontend-web/src/features/timeline/model/timelineOps.ts:561`). No video-capable package exists in `pubspec.yaml` yet, so probing a picked file's duration/dimensions needs a new dependency (e.g. `video_player`). Scope to append-only ingestion (muted by default, poster-frame preview) — own-audio mixing and frame-accurate scrub preview are separate, later web phases and explicitly out of scope here. | A video-capable Flutter package is added and used to probe a picked file's duration/dimensions before it's registered as a `MediaAsset`.<br>New pure `appendVideoClip`/`visualEnd` ops in `timeline_ops.dart` (mirroring web) append the clip to the base visual track after the current visual end.<br>"🎬 Add video" action on the Timeline tab picks a file, stores its blob via `TimelineAssetRepository`, and appends it; the clip shows a poster-frame thumbnail (not full scrub preview) in the editor and preview stage.<br>The existing generic `trimClip`/`splitClip` ops work unchanged on a video clip — verified, no video-specific branching needed.<br>A real `/render` export of a photo+video timeline plays real motion during the video's span, confirmed via frame-diffing.<br>Own-clip audio stays muted (`includeAudio: false` default) — explicitly not wired up in this task.<br>Unit tests cover the new ops; `flutter analyze` clean, `flutter test` green. | Hard |
| TASK-009 | ElevenLabs API key config-key mismatch between code and docs: `Program.cs:17` reads `builder.Configuration["ELEVENLABS_APIKEY"]` (flat key, unchanged since the initial commit per `git log -S`), but `README.md:36-51` and `CLAUDE.md:67-75` instruct `export ElevenLabs__ApiKey=...` or an appsettings `{"ElevenLabs":{"ApiKey":...}}` section — .NET maps both of those to config key `ElevenLabs:ApiKey`, which the code never reads (`_` ≠ `:` even case-insensitively). A fresh clone following the README crashes at startup with "ELEVENLABS_APIKEY is required". Gemini's flat style (`GEMINI_APIKEY`) is consistent between docs and code, so the repo currently mixes two conventions. | Pick one convention (recommend: code reads `ElevenLabs:ApiKey` first with `ELEVENLABS_APIKEY` as fallback, so both the documented setup and any existing env var keep working).<br>README.md and CLAUDE.md agree with the code exactly.<br>Backend verified to start using only the documented setup on a shell without the other variant set. | Easy |
| TASK-010 | `frontend-web` compiles without TypeScript strict mode: `tsconfig.app.json` and `tsconfig.node.json` have only `noUnusedLocals`/`noUnusedParameters`/`erasableSyntaxOnly`/`noFallthroughCasesInSwitch` — no `"strict": true` — while the sibling `design-system/tsconfig.json` in the same project does set `"strict": true` (and Vite's react-ts template ships strict by default). Implicit `any` and unchecked nulls are currently allowed in the app but not in the DS package. | `"strict": true` added to `tsconfig.app.json` and `tsconfig.node.json`.<br>All resulting type errors fixed (no blanket `any` casts to silence them).<br>`tsc -b && vite build` and `npm run lint` clean. | Medium |
| TASK-011 | `Greyvetro.slnx` (repo root) lists only the four production projects — `backend/Greyvetro.Tests/Greyvetro.Tests.csproj` is missing, so solution-level `dotnet build`/`dotnet test` and IDEs opening the solution skip the test suite entirely. | Tests project added to the `/backend/` folder in `Greyvetro.slnx`.<br>`dotnet build Greyvetro.slnx` builds all five projects.<br>`dotnet test` against the solution discovers and runs the existing 44 tests. | Easy |
| TASK-012 | `frontend-web` has zero committed tests: no test files anywhere (`find` over the tree returns none outside node_modules), no test script in `package.json`, no vitest/jest dependency — even though CLAUDE.md repeatedly cites "20/20 pure-ops assertions" / "16/16 audio-ops assertions" for `timelineOps.ts`, meaning those assertions were run ad hoc and discarded. Backend has a 44-test xUnit suite and Flutter has `test/timeline_ops_test.dart`; web owns the most complex pure logic (`timelineOps.ts`, ~600+ lines of ops) with no committed coverage. | Vitest (or equivalent) added as a devDependency with an `npm test` script.<br>The pure-ops assertions exist as committed test files covering at minimum `timelineOps.ts` (reanchor/move/trim/split/delete, audio ops, overlay ops, `mergeAddedMedia`) and `useTimelineHistory`'s coalescing.<br>`npm test` passes and is documented in `frontend-web/README.md`. | Medium |
| TASK-013 | Flutter project metadata is still scaffold debris: `pubspec.yaml:2` says `description: "A new Flutter project."` and the file keeps every generated comment block; `analysis_options.yaml` is the untouched template (only commented-out example rules, nothing enabled beyond stock `flutter_lints`). The backend and web packages both have real descriptions/configs. | `pubspec.yaml` has a real one-line description and the boilerplate comment blocks pruned (keep the fonts comment — it's real).<br>`analysis_options.yaml` trimmed of template comments; either enable a small deliberate rule set (e.g. `prefer_single_quotes`, `always_declare_return_types`) or record in CLAUDE.md that stock `flutter_lints` defaults are the standard.<br>`flutter analyze` clean. | Easy |
| TASK-014 | Dart model class naming is internally inconsistent and diverges from the sibling stacks: `VoiceModel` (`voice_model.dart:1`) and `UsageModel` (`usage_model.dart:1`) carry a `Model` suffix while `Transcript` (`transcript_model.dart:38` — a `_model.dart` file whose class has no suffix), `Preset`, `Project`, `GalleryItem`, `StoredScene`, `ScenePrompt`, and all timeline classes are bare nouns. TS (`core/types.ts`) and C# (`Domain/Entities/`) both use bare `Voice`/`Usage` for the same wire objects. | `VoiceModel` → `Voice` and `UsageModel` → `Usage` (files renamed to `voice.dart`/`usage.dart` or kept — pick one file-naming rule and note it), all references updated.<br>No remaining `Model`-suffixed class in `lib/` unless it genuinely disambiguates.<br>`flutter analyze` and `flutter test` clean. | Easy |
| TASK-015 | `frontend-web/README.md:29-38` "Structure" diagram is stale: it omits `features/script/`, `features/stt/`, `features/storyboard/`, `features/timeline/` (the app's largest feature), `core/db.ts`, `core/favorites.ts`, `core/Icon.tsx`, and the `design-system/` package; the parity section stops at Create-my-voice and never mentions Storyboard or the Timeline editor. | Structure block lists the actual `src/` tree (all 10 feature dirs + current core files) and mentions `design-system/` with one line on its regenerate-from-app build.<br>Parity/feature section covers Storyboard and Timeline.<br>No stale paths remain (spot-check every path named in the README exists). | Easy |
| TASK-016 | No `.editorconfig` anywhere in the repo (only jszip's own copy inside `node_modules`), so C#/Dart/TS indent and newline conventions are enforced by nothing — combined with web having no formatter (oxlint lints but doesn't format), formatting consistency is purely by habit across three stacks. | A root `.editorconfig` covering: C# 4-space + `dotnet` naming defaults, Dart 2-space, TS/TSX/CSS/JSON/YAML 2-space, trailing newline, UTF-8.<br>Values chosen to match the existing dominant formatting so the commit introduces no reformatting churn (verify with `git diff --stat` ≈ 1 new file).<br>Editors pick it up (VS Code needs no extra config beyond the checked-in `.vscode/`). | Easy |
| TASK-017 | The API contract is hand-mirrored in three languages with no drift guard: the Timeline model exists as C# records (`Domain/Entities/Timeline.cs`), TS (`frontend-web/src/features/timeline/model/types.ts`), and Dart (`frontend/lib/features/timeline/model/timeline.dart`), plus per-endpoint DTO parsing duplicated in `core/api.ts` and `core/api_client.dart`; a renamed/added field breaks a client only at runtime. The backend already wires `Microsoft.AspNetCore.OpenApi` and maps `/openapi` in dev (`Program.cs`), so a machine-readable contract exists but nothing consumes it. Also: web has a `VITE_API_BASE` override while Flutter's `ApiClient._base` (`api_client.dart:12`) is a hardcoded const. | A drift guard exists — either generated types (OpenAPI → TS + Dart) or serialization contract tests (golden JSON fixtures produced by the C# serializer that both frontends' parsers round-trip in their own test suites) — and fails CI/tests when a field changes in one place only.<br>`ApiClient._base` reads `String.fromEnvironment('API_BASE', defaultValue: 'http://localhost:5050')` to mirror web's `VITE_API_BASE`.<br>Approach and workflow documented in CLAUDE.md. | Hard |
